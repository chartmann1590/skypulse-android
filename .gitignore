*.iml
.gradle/
/local.properties
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Android Studio / IntelliJ
.idea/
*.hprof

# Gradle build output
**/build/

# Keystore files (never commit signing material)
*.jks
*.keystore

# Firebase config (contains the project API key) — injected from the
# GOOGLE_SERVICES_JSON secret in CI; keep your local copy untracked.
app/google-services.json

# Logs
*.log

# Local device screenshots used only for manual QA (may contain personal notifications)
docs/img/device_*.png
